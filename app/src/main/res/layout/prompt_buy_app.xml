<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/rounded_dialog">

    <FrameLayout
        android:id="@+id/text_frame"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginBottom="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="12dp"
            android:text="@string/buy_app"
            android:textAlignment="center"
            android:textColor="@color/colorBlack"
            android:textSize="22sp" />
    </FrameLayout>

    <ImageButton
        android:id="@+id/clear_buy_app_button"
        style="@style/yesNoButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentEnd="true"
        android:layout_margin="0dp"
        android:layout_marginBottom="0dp"
        android:layout_marginEnd="0dp"
        android:layout_marginStart="0dp"
        android:layout_marginTop="0dp"
        app:srcCompat="@drawable/cross_in_circle" />

    <ImageButton
        android:id="@+id/buy_app_button"
        style="@style/yesNoButton"
        android:layout_width="200dp"
        android:layout_below="@+id/five_stars"
        android:layout_alignBaseline="@+id/five_stars"
        android:layout_centerHorizontal="true"
        android:scaleType="fitCenter"
        android:textAlignment="center"
        app:srcCompat="@drawable/buy_app_button" />

    <ImageView
        android:id="@+id/five_stars"
        style="@style/yesNoButton"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/text_frame"
        android:layout_alignParentStart="false"
        android:layout_alignParentTop="false"
        android:layout_alignParentEnd="false"
        android:layout_alignParentBottom="false"
        android:layout_centerHorizontal="true"
        android:padding="5dp"
        android:scaleType="fitCenter"
        android:textAlignment="center"
        app:srcCompat="@drawable/unicorn" />


</RelativeLayout>